// This module has no request-body input to validate — both endpoints
// (GET /my, PATCH /:id/read) take everything from the URL/query, not the body.
// Kept as an empty file so the module still follows the standard 5-file shape.
export {};
